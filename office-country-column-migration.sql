-- ════════════════════════════════════════════════
--  Migration: إضافة عمود الدولة لجدول office_settings
-- ════════════════════════════════════════════════
--  المشكلة: الكود بيحفظ/يقرأ دولة المكتب (مصر/السعودية/...) من عمود
--  اسمه country داخل office_settings، لكن العمود ده مش موجود في
--  الجدول أصلاً — فعملية حفظ الدولة كانت فاشلة من زمان.
--
--  الافتراضي: مصر (EG) — لأي مكتب جديد أو صف قديم بدون قيمة.
-- ════════════════════════════════════════════════

ALTER TABLE office_settings
  ADD COLUMN IF NOT EXISTS country text DEFAULT 'EG';

-- تأكيد إن أي صف قديم موجود فعلاً بدون قيمة يبقى مصر كافتراضي صريح
UPDATE office_settings
SET country = 'EG'
WHERE country IS NULL;
